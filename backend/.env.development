# HR System Environment Variables - Development

# Application Settings
NODE_ENV=development
PORT=5455

# MongoDB Settings (Development)
# ⚠️ SECURITY: Never commit actual MongoDB Atlas credentials
# Set your MongoDB Atlas URI in .env file:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017  # Default to local for security
DB_NAME=SM_nomu

# Legacy local MongoDB (comment out when using Atlas)
# MONGODB_URL=mongodb://localhost:27017

# Session Configuration
SESSION_SECRET=hr-development-secret-2025